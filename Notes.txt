Git server location:
\\cas-fs1.uoregon.edu\Material-Science-Institute\Aleman-Lab\Group\Git Repositories\bc\CouplingDataAnalysis

Git commands:
git branch -r | grep -v '\->' | while read remote; do git branch --track "${remote#origin/}" "$remote"; done 
= learn about someone else's branches on the server
git fetch --all = take a look at the files from the server (won't change local files)
git pull --all = get the files from the server (will change your local files!)

git stage *
git commit -m "message"
git push --all